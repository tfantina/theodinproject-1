<!DOCTYPE html>
<html lang="en">
  <%= render 'shared/head' %>

  <body>
    <% if current_page?(controller: 'users', action: 'show') %>
    <div id="tooltip" style="position: absolute; visibility: hidden; z-index: 500"></div>
    <% end %>

    <div id="global-content-wrapper">
      <%= render 'shared/navbar_and_flashes' %>
      <%= yield %>
    </div>

    <%= render 'shared/footer' %>
    <%= render 'shared/sidecar' %>
    <%= render 'shared/ga' %>
    <%= render 'shared/fb' %>
  </body>
</html>
