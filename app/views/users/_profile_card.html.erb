<div class="card-main profile-card">
  <div class="profile-card-row1">
    <%= image_tag avatar_path(user.avatar), class: 'profile-card__image' %>
    <h2 class="profile-card__username"><%=  user.username %></h2>
    <p class="profile-card__learning-goal"><%= display_dashboard_learning_goal(user) %></p>

    </div>
    <div class="profile-card-row2">
        <%= render 'calendar', active_days: @active_days %>
    </div>
</div>
